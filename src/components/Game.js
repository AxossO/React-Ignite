import React from "react";


const Game = () => {
 
  return (
    <div className="game">
      {/* {popularGames.map((game) => (
        <div key={game.id}>{game.name}</div>
      ))} */}
      hello
    </div>
  );
};

export default Game;
